/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./*.html'], // This tells Tailwind to scan all HTML files in the root directory
  theme: {
    extend: {},
  },
  plugins: [],
}